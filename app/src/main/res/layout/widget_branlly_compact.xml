<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_panel_background"
    android:padding="8dp">

    <TextView
        android:id="@+id/widget_center"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="center"
        android:background="@drawable/widget_center_background"
        android:contentDescription="@string/widget_open_menu"
        android:gravity="center"
        android:text="B"
        android:textColor="#E5E8FF"
        android:textSize="28sp"
        android:textStyle="bold" />
</FrameLayout>
